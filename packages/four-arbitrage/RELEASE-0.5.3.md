# 0.5.3 — 展示双方向下单门槛

在总览与交易组合页面的两个方向信号旁增加下单门槛（bps）。门槛为反方向平均价格差的相反数，按本方向卖方买一价及折算率换算为 bps，再加目标 bps。没有均值样本时显示“待采样”。

下单信号仍与目标 bps 比较；门槛对应价格差的入场边界，不能直接拿偏离信号与它比较。此次只增加展示，不修改执行判断、仓位限制或对账逻辑。

验证：163 项相关回归测试；两个方向门槛换算与实际触发判断一致性检查；封包适配器检查及管理中心代理浏览器检查。未发送真实交易订单。
