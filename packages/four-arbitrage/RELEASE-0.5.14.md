# 0.5.14 — 配置保存不再按运行和持仓状态拦截

保存平台配置不再要求先暂停、清空持仓或消除待恢复状态。若已有订单执行/对账，保存请求等待同一串行锁后合并并应用配置，避免并发替换交易账户。专用 Arcus 密钥保存按钮也移除前端暂停检查。

应用配置时暂停新增交易；保留原订单、成交及持仓账本，有持仓或未确认订单则要求重新核对后恢复。字段格式、组合配置一致性、2FA 和新 Arcus 密钥有效性校验保留。

验证：439 项自动化测试通过，覆盖运行中/持仓期间保存、配置等待执行锁、账本保留、对账恢复要求和密钥失败不覆盖旧配置；封包管理中心浏览器检查通过。未发送真实交易订单。
