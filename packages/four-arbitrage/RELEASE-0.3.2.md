# 混合套利节点 0.3.2

参数错误改为提示具体字段，例如滑动窗口样本、每腿数量或各交易所组合目标；不回显输入值或账户密钥。

同时更新线上管理中心的脚本转发规则：不再改写 JavaScript 中单独的 `/` 字符串。旧规则误改写组合标识的分隔符，导致从管理中心页面提交生成组合时，合法目标被拒绝。此根因修复属于管理中心，单独更新节点不能修复旧版管理中心。

按用户截图的 BTC、数量 0.001、窗口 20000、预热 100、各组合 1/3/2.5/3/2.5/6 bps 验证通过。管理中心路径改写测试、字段错误测试通过，并通过页面按钮实际提交生成组合的代理浏览器验证。未替用户创建策略，未发送实盘订单。
