# 混合套利节点 0.2.1

- 监控显示最新已知盘口，不再因为两边相差超过 500ms 或超过 1.5 秒就统一显示等待行情；明确标注延迟与暂不可执行。无首次报价时仍等待行情。
- 时间判断以服务器快照时间为基准，不依赖用户电脑时钟。真实下单仍保留严格行情时效与双边时间差检查。
- 矩阵与固定价差信号直接使用（卖方买一 / 买方卖一 − 1）× 10000，不扣预估手续费或滑点。滑动策略仍按原双腿的双向均值修正公式判断。
- 成交收益继续记录实际成交手续费；滑点仍用于下单限价保护及模拟成交。
- 预热状态保留布尔类型，避免字符串 False 被页面当成已完成预热。

84 项测试通过，覆盖延迟报价可见但禁止执行、费用参数不改变价差、预热布尔值。封包浏览器验证包含电脑时钟偏差、延迟标签、费用不影响价差。未发送实盘订单。
