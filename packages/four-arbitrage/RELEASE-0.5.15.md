# 0.5.15 — Arcus 密钥校验返回具体原因

保存 Arcus 密钥失败时返回实际核验原因，不再仅显示注册状态/有效期/范围/签名的汇总提示。子账户不匹配时明确列出 API Key 的适用子账户和节点配置的子账户。

错误信息同时脱敏已保存凭据与本次尚未保存的新凭据，核验失败仍不修改原配置。

验证：14 项密钥更新专项测试通过，包含新提交未保存私钥的脱敏；封包管理中心浏览器检查通过。未发送真实交易订单。
