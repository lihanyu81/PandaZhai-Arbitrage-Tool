# 四平台套利节点 0.1.1

同步 QFEX–Lighter 节点的成交确认与收益回补修复。

- 兼容 QFEX 鉴权回报及 `user_trades_response`；成交明细缺少客户端订单号和时间戳时，以已核验的交易所订单号、标的、方向和历史成交时间关联。
- 兼容 `orders / twaps` 挂单响应；缺失订单列表仍视为读取失败。
- 每条腿独立限时等待并立即保存结果；另一腿超时不会抹掉已经确认的成交。提交前保存所有客户端订单号。
- 每 30 秒补查未确认订单，完整成交返回后自动更新成交账本和收益；不重发订单，不自动恢复交易，仍需核对持仓并恢复。
- 保留新鲜盘口平仓取价、动态合约精度、IOC 限价、reduce-only 和脱敏拒单原因。

验证：39 项自动测试通过；封包自检与演示模式浏览器测试通过。不执行实盘验收订单。

默认演示模式且暂停。独立节点版，安装路径与双腿节点分开。升级前请处理现有任务并备份数据。

发行包不含散装 Python 应用源码、账户配置或数据库；字节码嵌入可执行文件，不能保证防止专业反编译。
