# 0.5.13 — Arcus 密钥专用保存入口

平台接入的 Arcus 卡片增加“仅保存 Arcus API 密钥”按钮，只提交 API Key 和签名私钥，继续要求 2FA、暂停和服务端新密钥核验。不提交其他平台、账户身份、步数、折算率或滑点字段，避免整表保存携带无关变化而被持仓保护拦截。

持仓配置比较改为类型化数值比较，平台顺序不视为账户变化。真实的非密钥变化仍阻止，报错列出具体字段路径，不回显凭据值。

验证：13 项密钥更新专项测试通过，覆盖等价数值/平台顺序与错误脱敏；封包管理中心浏览器验证专用按钮仅提交两项密钥，即使其他表单字段有改动也不携带。未发送真实交易订单。
